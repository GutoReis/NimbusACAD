@model IEnumerable<NimbusACAD.Models.ViewModels.ListarNotasAlunoViewModel>

@{
    ViewBag.Title = "VerNotasAluno";
}

<h2>VerNotasAluno</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MatriculaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MatriculaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MatriculaID })
        </td>
    </tr>
}

</table>
